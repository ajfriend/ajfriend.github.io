# Tools TODO

## Codenames

- iPadOS Safari doesn't support the Fullscreen API. Alternative is "Add to Home Screen" which runs it as a standalone web app. In that case, the wake lock should always be active instead of tied to fullscreen.
