[project]
name = "h3-bits"
version = "0.0.1"
requires-python = ">=3.14"
dependencies = [
    "h3",
    "matplotlib",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/h3_bits"]
